<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ucajosuecort.proyectopdm0120">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ModifyGradesRecord" />
        <activity android:name=".Schedule" />
        <activity android:name=".WeeklySchedule" />
        <activity android:name=".Record" />
        <activity android:name=".ModifyPensum" />
        <activity android:name=".Pensum" />
        <activity android:name=".UpdatePassword" />
        <activity android:name=".Profile" />
        <activity android:name=".Home" />
        <activity android:name=".sing_up" />
        <activity android:name=".sing_in" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>